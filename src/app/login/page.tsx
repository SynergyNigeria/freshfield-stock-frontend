import LoginContent from "./LoginContent";

export const metadata = {
  title: "Freshfield — Sign In",
  description: "Sign in to your Freshfield trading account.",
};

export default function LoginPage() {
  return <LoginContent />;
}
